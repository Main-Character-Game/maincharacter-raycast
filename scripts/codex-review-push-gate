#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
shared_scripts_dir="$($repo_root/scripts/resolve-shared-scripts-dir.sh)"
exec "$shared_scripts_dir/codex-review-push-gate" "$@"
